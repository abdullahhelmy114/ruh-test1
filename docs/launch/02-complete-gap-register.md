# 02 — Complete gap register

Revision under review: **70a77e2** (local) on top of **1de8fa2** (deployed). Prepared 2026-09-21.

The authoritative register for this review: **136 findings** from six read-only investigations of
this revision. Two areas — security and public claims — were re-checked finding by finding against
the files by an adversarial verifier (32 and 23 findings; 0 dropped). The rest are single-pass: real
leads, read from the code, but not independently re-checked. The "State" column says which, and the
items I personally verified are called out in reports 07, 09, 10 and 11.

An earlier register of 125 findings covers the unavailable-features audit of 1de8fa2 and remains
valid: `docs/audits/2026-09-21-unavailable-features.md`. What has been fixed since is in report 03.

By severity: P0 8, P1 51, P2 52, P3 25.
By area: claims 23, i18n-a11y 24, journeys 18, policies 19, security 32, whop 20.
By classification: APPROVED FUTURE WORK 2, BLOCKED BY CONFIGURATION 4, BLOCKED BY REQUIRED DATA 11, BLOCKED BY SCHEMA 3, BROKEN LEGACY FEATURE 8, FIXED LOCALLY 2, INCOMPLETE IMPLEMENTATION 66, INTENTIONALLY RETIRED 1, OWNER DECISION REQUIRED 10, SECURITY DEFECT 6, WORKING 23.

**Nothing here is marked complete.** A finding closes only against a verification record in report
12 (local and isolated tests) or report 13 (production). Dependencies, rollback and the owner
decision for each P0/P1 item are carried in reports 04, 09, 10, 11 and 15.

| ID | Feature | Route / API | Role | Observed | Expected | Root cause | Evidence | Sev | Fix | Owner decision | Classification | State |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G001 | Central authentication and identity resolution | All API routes; src/lib/auth/core.ts, src… | system | Every cited line checks out. `extractCredential` reads only the Authorization header and the `__session` cookie (core.t… | Identity and role come only from a verified credential and the stored profile; never… | n/a - correct by design. | src/lib/auth/core.ts:152-174, :198-227, :235-244, :253-259; src/lib/auth/index.ts:36… | P0 | None. | no | WORKING | confirmed |
| G002 | Role guards on the administrative surface | src/app/api/admin/** (106 route files, no… | admin | the invariant holds, the count was wrong. I re-ran the scan: `find src/app/api/admin -name route.ts` returns 106 files,… | Every administrative endpoint requires an admin session server-side. | n/a. | Scripted scan reproduced: 106 route.ts files under src/app/api/admin, 0 without a gu… | P0 | None. | no | WORKING | corrected |
| G003 | Object-level authorisation in the academy (class group,… | src/app/api/academy/** via src/lib/academ… | student | traced end to end and every cited line is accurate. Teacher access requires both `classGroupBelongsToCourse` and `isTea… | A signed-in user cannot read or write another user's object by substituting an id. | n/a. | src/lib/academy/permissions/permissions.ts:105-213; src/lib/academy/repo/relationshi… | P0 | None. | no | WORKING | confirmed |
| G004 | Privacy of student private notes (annotations) versus s… | /api/academy/annotations/[annotationId]; … | student | CONFIRMED, with the invariant stronger than stated. `annotation.read`/`annotation.modify` return `deny('privacy')` unle… | Private notes stay private to their author, including from administrators, and are s… | n/a. | src/lib/academy/permissions/permissions.ts:182-187; src/lib/academy/repo/lesson-repo… | P0 | None. | no | WORKING | confirmed |
| G005 | Session cookie lifecycle and sign-out revocation | POST/DELETE /api/auth/session | system | CONFIRMED. `SESSION_COOKIE = { httpOnly: true, secure: true, sameSite: 'lax', path: '/' }` (session/route.ts:8) is spre… | A session cookie is httpOnly, secure, same-site, expires, and sign-out ends it on ev… | n/a. | src/app/api/auth/session/route.ts:6-8, :33-38, :58-71; src/lib/auth/index.ts:41-45. | P0 | None. (14 days is the Firebase maximum; shortening it is an owner choice, not a defe… | no | WORKING | confirmed |
| G006 | Whop webhook signature verification, replay window and … | POST /api/webhooks/whop | system | CONFIRMED in full. The handler fails closed with 500 when `whopConfig.webhookSecret` is null (route.ts:26-30), reads `a… | Only Whop-signed, fresh, once-processed events may change academy access. | n/a. | src/app/api/webhooks/whop/route.ts:25-65; src/lib/payments/whop.ts:12-20, :26, :73-8… | P0 | None. | no | WORKING | confirmed |
| G007 | SQL parameterisation | Whole codebase | system | CONFIRMED; I reproduced the scan independently and it returns nothing. `grep -rn -A6 '\.query(`' src | grep '${'` produ… | No user-controlled value is ever concatenated into SQL text. | n/a. | src/lib/academy/infra/sql.ts:23-60; src/lib/academy/repo/lesson-repo.ts:188-202; src… | P0 | None. | no | WORKING | confirmed |
| G008 | Secret handling and client-bundle exposure | Whole codebase | system | CONFIRMED; I reproduced the scan. Over every file containing a `'use client'` directive, a grep for `process.env.<NAME>… | No secret in a client bundle, a URL, a log line or an error body. | n/a. | Scan over all 'use client' files for non-NEXT_PUBLIC env reads: zero hits. src/lib/f… | P0 | None. | no | WORKING | confirmed |
| G009 | Accreditation, institutional-trust and "certified cours… | / (certification band), /certification (p… | public | CONFIRMED, every citation checked and exact apart from the homepage lines that 8908c57 shifted. src/app/certification/p… | Accreditation and institutional-recognition claims are made only where a named accre… | Marketing copy written ahead of any accreditation. | src/app/page.tsx:358, :371-376, :406-410 (HEAD 8908c57; e257693 :396, :409-414, :444… | P1 | NOT LOCAL to keep, LOCAL to withdraw. Replace with verified approved information if … | yes — name the accrediting body and… | OWNER DECISION REQUIRED | confirmed |
| G010 | /subscriptions — invented plan prices and a "Buy Now" c… | /subscriptions | public | CONFIRMED on the facts, CORRECTED on the fix. src/app/subscriptions/page.tsx:10-13 is `const defaultPlans = [{ id: '1',… | No price is shown that the owner has not approved and that no payment service can ch… | Legacy subscription page whose literals were never sourced from a service; subscript… | src/app/subscriptions/page.tsx:10-13, :37, :39, :41-50; src/app/api/subscriptions/ro… | P1 | LOCAL, but it is a two-file change, not one. Replace src/app/subscriptions/page.tsx … | no — the blueprint already says pla… | BROKEN LEGACY FEATURE | confirmed |
| G011 | /about renders raw translation keys instead of content | /about | public | CONFIRMED, and I re-derived it independently rather than trusting the claim. src/components/TranslatedText.tsx:19-22 is… | The About page shows the academy's real description. | `<T>` resolves only flat top-level string keys (src/components/TranslatedText.tsx:19… | src/app/about/page.tsx:61, :65, :69, :82, :85, :94, :97, :107, :110, :124, :127, :14… | P1 | LOCAL. Either add the flat keys `<T>` expects to src/messages/{en,ar,tr}.json, or ch… | yes — the About copy itself must be… | INCOMPLETE IMPLEMENTATION | confirmed |
| G012 | /certification renders raw translation keys instead of … | /certification (linked from the primary n… | public | CONFIRMED. src/app/certification/CertificationContent.tsx emits `<T>` at :34 Certification Badge, :38 Certification Hea… | The certification page explains the certificate. | Same `<T>` key fallback (src/components/TranslatedText.tsx:19-22) with the real copy… | src/app/certification/CertificationContent.tsx:34, :38, :42, :57, :61, :101, :104, :… | P1 | LOCAL, with a content review first. Wire the keys, but drop `certificationPage.value… | yes — the printed-certificate deliv… | INCOMPLETE IMPLEMENTATION | confirmed |
| G013 | Legal pages publish no legal text — /terms, /privacy, /… | /terms, /privacy, /terms-of-sale, /refund… | public | CONFIRMED. src/app/refund-policy/page.tsx:10-15 is the four-section array (`Refund General`, `Refund Law`, `Refund Exce… | A site that links a refund policy, terms of sale, terms of service and a privacy pol… | Same `<T>` key-fallback defect. Drafts exist unused under `termsPage` and `privacyPa… | src/app/refund-policy/page.tsx:10-15, :25, :28, :31, :47, :50; src/app/terms-of-sale… | P1 | NOT LOCAL. The Sale* and Refund* text exists nowhere in the repository and must be s… | yes — approved refund, sale, terms … | INCOMPLETE IMPLEMENTATION | confirmed |
| G014 | /signup sells capabilities the product does not current… | /signup | public | CONFIRMED, every line exact. src/app/signup/page.tsx:33 "Access hundreds of Arabic course and live sessions with the be… | The sign-up page describes what an account actually gets today. | Copy written for the intended catalogue; the community bullet outlived the feature's… | src/app/signup/page.tsx:33, :53-69; src/app/community/page.tsx:1-8; src/app/academy/… | P1 | LOCAL. Remove the "hundreds of" magnitude at :33 and :56 and the "Community of learn… | no for the community bullet (the fe… | INCOMPLETE IMPLEMENTATION | confirmed |
| G015 | Primary navbar advertises a Dictionary that cannot load | /dictionary (navbar item, src/components/… | public | CORRECTED on line references, confirmed on substance. "Dictionary" is the fourth entry of `baseLinks` at src/components… | A top-level navigation item leads to a feature that works, or is not shown. | Legacy dictionary retained in the primary navigation after its tables were left out … | src/components/Navbar.tsx:31-36 (:35 `/dictionary`), :72; src/app/dictionary/page.ts… | P1 | LOCAL for the navigation. Temporary removal pending owner approval: drop the `/dicti… | yes — is the dictionary an approved… | BLOCKED BY SCHEMA | corrected |
| G016 | Localisation — the `<T>` translation component resolves… | /about, /certification, /privacy, /terms,… | public | `<T>` does a single flat top-level lookup (`dictionaries[locale][key]`) and falls back to the key string itself when th… | Every `<T>` key resolves to a translated string in all three product locales; a miss… | Two incompatible key schemes. The message files were authored with nested namespaces… | src/components/TranslatedText.tsx:19-22 `function translate(locale, key) { const val… | P1 | LOCAL. Either (a) make `translate()` walk dot paths and rewrite the component keys t… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G017 | Legal pages — /privacy and /terms render their entire b… | /privacy, /terms | public | /privacy maps nine sections over `{ key: "Information We Collect" }` etc. and renders `<T>{key}</T>` as the heading and… | A visitor reading /privacy and /terms gets the actual privacy notice and terms of se… | Same flat-vs-nested key mismatch as the `<T>` defect, applied to a page whose entire… | src/app/privacy/page.tsx:29-39 the `sections` array; :80-86 `<h2 ...><T>{key}</T></h… | P1 | LOCAL for the wiring (point the page at the ids that hold the copy, or flatten the f… | yes — the actual privacy-policy and… | INCOMPLETE IMPLEMENTATION | single-pass |
| G018 | SEO — the root layout's canonical URL is inherited by r… | all public routes that do not set their o… | public | src/app/layout.tsx:34-41 sets `alternates: { canonical: "https://ruhulqudus.com", languages: { en: .../en, ar: .../ar, … | Each indexable route declares its own address as canonical, so the academy catalog, … | A site-wide canonical was placed in the root layout instead of per route. Next.js me… | src/app/layout.tsx:34-41; src/app/academy/courses/[slug]/page.tsx:35-44 generateMeta… | P1 | LOCAL. Set `metadataBase: new URL("https://ruhulqudus.com")` in the root layout and … |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G019 | SEO / localisation — hreflang alternates advertise /en,… | every page that inherits the root alterna… | public | The root layout declares `languages: { en: "https://ruhulqudus.com/en", ar: "https://ruhulqudus.com/ar", tr: "https://r… | hreflang either points at URLs that serve that language, or is not emitted at all. A… | next-intl was configured (src/i18n/routing.ts sets `localePrefix: 'as-needed'`, src/… | src/app/layout.tsx:36-40; src/i18n/routing.ts:1-8; `find . -name "middleware.*" -not… | P1 | LOCAL. Remove the `languages` block from src/app/layout.tsx:36-40 (and delete the un… | yes — whether the site keeps cookie… | INCOMPLETE IMPLEMENTATION | single-pass |
| G020 | SEO — two conflicting sitemaps and two conflicting robo… | /sitemap.xml, /robots.txt | public | public/sitemap.xml and src/app/sitemap.ts both resolve to /sitemap.xml, and public/robots.txt and src/app/robots.ts bot… | One source of truth per file. Whichever wins, the crawler directives the owner inten… | Static files were left in public/ when the dynamic metadata routes were added. Both … | public/sitemap.xml (11 URLs, no /quran, no /library); src/app/sitemap.ts:8-22 (14 UR… | P1 | LOCAL. Delete public/sitemap.xml and public/robots.txt and keep the app-router versi… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G021 | Localisation — the whole public site renders in English… | every route that uses `<T>` (home, /about… | public | src/app/layout.tsx:88-91 reads the `preferred-locale` cookie on the server and correctly emits `<html lang="ar" dir="rt… | The document language, direction and text agree from the first byte. The academy sid… | `<T>` was built as a client-only component with a localStorage source, predating the… | src/components/TranslatedText.tsx:24-40 `function usePreferredLocale() { const [loca… | P1 | LOCAL. Pass the server-resolved locale down (a provider seeded from the layout's coo… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G022 | Localisation — changing the language does not re-render… | global (the switcher lives in the header … | public | LanguageSwitcher writes localStorage, writes the cookie, sets `document.documentElement.dir`/`lang`, and dispatches a `… | Choosing a language changes the language of the page the reader is looking at, in ev… | Three independent translation mechanisms coexist with no shared source of truth: (1)… | src/components/LanguageSwitcher.tsx:55-63 `changeLocale` — no `router.refresh()`; sr… | P1 | LOCAL. Call `router.refresh()` in src/components/LanguageSwitcher.tsx:55-63 after wr… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G023 | Colour contrast — the primary Sign In button's label is… | /login, /live/login | public | The submit button is `bg-gradient-gold ... text-primary-foreground`. In light mode `--primary-foreground` is oklch(0.98… | The label on the primary submit control of the sign-in form meets WCAG 2.1 AA (4.5:1… | `text-primary-foreground` was paired with a gold background. `--primary-foreground` … | src/app/login/page.tsx:178-181 `className="... bg-gradient-gold py-3.5 text-sm font-… | P1 | LOCAL. Change `text-primary-foreground` to `text-gold-foreground` on src/app/login/p… |  | SECURITY DEFECT | single-pass |
| G024 | Colour contrast — the `.text-gold` utility fails AA aga… | global (footer on every page, /about, /ce… | public | `.text-gold` sets `color: var(--gold)`, which in light mode is oklch(0.80 0.15 85) ≈ #eab532. Against `--background` (#… | Body and small text meets WCAG 2.1 AA (4.5:1) in both themes. | `--gold` was tuned as a decorative/brand fill (its `--gold-foreground` partner is da… | src/app/globals.css:75 `--gold: oklch(0.80 0.15 85);`, :177 `.text-gold { color: var… | P1 | LOCAL. Add a text-specific gold token, e.g. `--gold-text: oklch(0.52 0.13 80)` for l… |  | SECURITY DEFECT | single-pass |
| G025 | Keyboard and screen reader — the public site has no ski… | every public and legacy route (the root l… | public | The root layout renders `<Navbar />` then `<main className="flex-1">{children}</main>` with no id and no preceding skip… | WCAG 2.1 SC 2.4.1 Bypass Blocks: a mechanism to skip repeated blocks. The academy wo… | The skip link was added when the academy workspace shell was built and never lifted … | src/app/layout.tsx:106-110 `<div className="relative flex min-h-screen flex-col"><Na… | P1 | LOCAL. Add `id="main"` and `tabIndex={-1}` to src/app/layout.tsx:108's `<main>` and … |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G026 | Keyboard and screen reader — the mobile navigation draw… | every route, below the xl breakpoint (pho… | public | The drawer renders as `{mobileOpen && <div className="xl:hidden fixed inset-0 z-50">...}`. It carries no `role="dialog"… | A modal overlay that covers the viewport is exposed as a dialog, traps focus while o… | The drawer was hand-rolled as a conditional div rather than built on the Radix Dialo… | src/components/Navbar.tsx:418-421 `{mobileOpen && (<div className="xl:hidden fixed i… | P1 | LOCAL. Rebuild the drawer on src/components/ui/sheet.tsx (Radix Dialog), or add `rol… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G027 | Form feedback — the contact form's inputs are unlabelle… | /contact | public | All three fields render a `<label>` with no `htmlFor` beside an `<input>`/`<textarea>` with no `id`, no `name` and no `… | Every control has an accessible name; validation errors are announced and tied to th… | The legacy public forms predate the workspace UI kit and were written without the la… | src/app/contact/ContactContent.tsx:248-251 `<label className="mb-1.5 flex items-cent… | P1 | LOCAL. Give each input an `id`, point the label at it with `htmlFor`, mark the requi… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G028 | Form feedback — the login form's errors are not announc… | /login, /live/login | public | The error block is `{error && <div className="flex items-center gap-2 rounded-lg bg-destructive/10 p-3 text-sm text-des… | Sign-in failures are announced to assistive technology, phrased in the reader's lang… | The page was written before the workspace's `FailureNotice`/`Notice` pattern and its… | src/app/login/page.tsx:169-173 the error block; :22 `const NO_ACCOUNT_MESSAGE = "Thi… | P1 | LOCAL. Add `role="alert"` to the error container, map Firebase error codes to transl… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G029 | RTL — the layout is written in physical directions, so … | every legacy and public route; the academ… | public | The root layout flips `dir` correctly, but 265 physical-direction utilities (`ml-*`, `mr-*`, `pl-*`, `pr-*`, `left-*`, … | Logical properties throughout, so one stylesheet serves both directions — the rule t… | The legacy site was authored LTR-first and `dir` support was retrofitted at the docu… | Counts by grep over src/**/*.tsx: physical 265, logical 23, `rtl:` variants 4. src/c… | P1 | LOCAL but large. Convert `ml/mr/pl/pr` → `ms/me/ps/pe` and `left-N/right-N` → `start… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G030 | Approval gate review (human review gates for academic/r… | /academy/approvals and /academy/approvals… | teacher | The approvals list shows only the gate type and the raw subject kind, and the gate detail page shows the subject as a b… | A reviewer opening an approval request sees what is under review — at minimum the su… | The gate read model was built around the gate record alone; no join to the subject's… | src/app/academy/(workspace)/approvals/page.tsx:25 `cell: (gate) => `${gate.subject.k… | P1 | PARTIALLY LOCAL. Local part: have `getGate`/`listOpenGates` join the subject's label… | yes — whether a teacher decider may… | INCOMPLETE IMPLEMENTATION | single-pass |
| G031 | Course readings / library resources (admin authoring an… | /academy/manage/courses/[courseId]?tab=re… | admin | Adding a reading requires the administrator to type a raw library book UUID into a free-text field; there is no book pi… | An administrator can choose a book from a list the academy owns, sees its title in t… | Migration 0006 deliberately made `academy_course_resources.library_book_id` an infor… | src/components/academy/workspace/admin/course.tsx:245-246 raw `TextInput id="reading… | P1 | NOT LOCAL for the data path — the academy has no book table of its own and the legac… | yes — whether the library becomes a… | BLOCKED BY SCHEMA | single-pass |
| G032 | Academy settings (policies) administration | /academy/manage/policies — GET /api/admin… | admin | Every one of the 13 policies is configured through a bare JSON `<textarea>`. The screen shows the policy key, the scope… | The policies screen is enough on its own to configure the academy: each setting stat… | The policy registry was designed as a validator only; no presentation metadata (desc… | src/app/academy/(workspace)/manage/policies/page.tsx:174 the value editor is `<TextA… | P1 | LOCAL. Add `description` and `example` (a literal valid value) to each `PolicyDefini… | no — the shapes are already fixed i… | INCOMPLETE IMPLEMENTATION | single-pass |
| G033 | Session, assignment and result times across the whole s… | /academy/learn, /academy/teach, /academy/… | student | When the `institution.timezone` policy has no value — which is the production state — `academyDisplayTimeZone()` return… | When the academy clock is not configured, learner and teacher screens say so once, p… | `formatDateTime`/`formatSessionTime`/`formatDate` return `""` for a null zone by des… | src/app/academy/(workspace)/layout.tsx:22 `const timeZone = await academyDisplayTime… | P1 | LOCAL. Expose `timeZone === null` from the workspace context (it already is) and ren… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G034 | Grading a submitted attempt (teacher evaluation) | /academy/attempts/[attemptId] — GET/PATCH… | teacher | The grader view lists each answer as "Item <raw itemId>" followed by the learner's response text. The question prompt, … | The grading screen shows each item's prompt (and, for graders, its rubric or answer … | The attempt read model was kept minimal and deliberately separate from assessment co… | src/app/academy/(workspace)/attempts/[attemptId]/page.tsx:179 and :100 both render `… | P1 | LOCAL. On the attempt page, fetch `api.assignment(view.assignmentId)` alongside the … |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G035 | Teacher application decision notification | PATCH /api/admin/academy/teacher-applicat… | applicant | `teacherService.decide` writes the decision, the history event and the audit event, and activates the account on approv… | An applicant is told when their application is decided — at minimum an email, since … | The decision path was built as a pure state transition; no outbound channel was wire… | src/lib/academy/services/teacher-service.ts:283-305 `decide` — statements are the st… | P1 | LOCAL. In `decide`, after the transaction commits, send an email with the existing `… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G036 | Email verification landing (student sign-up → verificat… | /verify-email → POST /api/verify-email-co… | student | Student sign-up creates the Firebase account server-side only; the browser never signs in. After the six-digit code is … | After verifying, the learner either lands signed in, or is taken to the sign-in scre… | The redirect target was changed to the role-derived home without the client ever hav… | src/app/signup/student/page.tsx:65-88 posts to /api/signup/student then `router.push… | P1 | LOCAL. Either (a) send the verified student to `/login?verified=1` the way /verify-t… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G037 | institution.timezone — the academy's single clock | Set: PATCH /api/admin/academy/policies/in… | admin|teacher|stude… | UNSET in production, so every path below fails. Schema: a bare JSON string holding an IANA zone name; parseTimezone rej… | An administrator sets it once, before any session is scheduled and before any learne… | No default by design and no migration seeds academy_policy_values (verified: no INSE… | src/lib/academy/policies/registry.ts:180-189,265-269; src/lib/academy/services/polic… | P1 | No code change. An administrator PATCHes {action:"set", scope:"academy", scopeId:nul… | yes — the owner must state the acad… | BLOCKED BY REQUIRED DATA | single-pass |
| G038 | attendance.vocabulary — the marks a teacher may record | GET + PUT /api/academy/sessions/[sessionI… | teacher|admin|stude… | UNSET. Schema: {marks:[{code, countsAsAttended, labels:{en,ar,tr}}]}, 1 to 12 marks; code must match /^[a-z][a-z0-9_]{0… | The academy defines its own mark vocabulary once, in all three product locales, and … | No default and no seed. | src/lib/academy/policies/registry.ts:191-218,270-274; src/lib/academy/services/atten… | P1 | No code change; set at academy scope. PROPOSED INITIAL VALUE: {"marks":[{"code":"pre… | yes — the codes are academic vocabu… | BLOCKED BY REQUIRED DATA | single-pass |
| G039 | learning.late_work — late submissions | POST /api/academy/assignments/[assignment… | student | UNSET, so a learner cannot start OR submit an attempt: both paths await policies.lateWork() unguarded and get 503 "This… | The academy states whether work is accepted after the due time, for how long, and wh… | No default and no seed. | src/lib/academy/policies/registry.ts:284-295; src/lib/academy/services/assessment-se… | P1 | No code change; set at academy scope. PROPOSED INITIAL VALUE: {"acceptLateSubmission… | yes — the lateness window is an aca… | BLOCKED BY REQUIRED DATA | single-pass |
| G040 | learning.revision — resubmission after a return | POST /api/academy/assignments/[assignment… | student|teacher|adm… | UNSET, so starting an attempt (assessment-service.ts:232) and returning one for revision (assessment-service.ts:331) bo… | The academy states whether a returned piece of work may be resubmitted and how often. | No default and no seed. | src/lib/academy/policies/registry.ts:296-307; src/lib/academy/services/assessment-se… | P1 | No code change. PROPOSED INITIAL VALUE: {"revisionAllowed":true,"maxRevisions":1}. R… | yes — how many second chances a lea… | BLOCKED BY REQUIRED DATA | single-pass |
| G041 | assessment.attempt_limit — how many attempts a learner … | POST /api/academy/assignments/[assignment… | student | UNSET, so starting any attempt is 503 (assessment-service.ts:231). Schema: {maxAttempts: whole number 1..100 or null}; … | The academy states a default attempt ceiling, overridable per program or course (a p… | No default and no seed. | src/lib/academy/policies/registry.ts:308-316; src/lib/academy/services/assessment-se… | P1 | No code change. PROPOSED INITIAL VALUE at academy scope: {"maxAttempts":1}, then a c… | yes — an academic ruling, and 1 vs … | BLOCKED BY REQUIRED DATA | single-pass |
| G042 | assessment.result_release — when a learner sees a result | PATCH /api/academy/attempts/[attemptId] {… | student|teacher|adm… | UNSET, so submitting an attempt (assessment-service.ts:261) and grading one (assessment-service.ts:307) both return 503… | The academy states when results become visible, consistent with the blueprint's huma… | No default and no seed. | src/lib/academy/policies/registry.ts:135-140,317-325; src/lib/academy/services/asses… | P1 | No code change. PROPOSED INITIAL VALUE: {"mode":"after_teacher_review"}. Reasoning: … | yes — it changes what a learner see… | BLOCKED BY REQUIRED DATA | single-pass |
| G043 | completion.criteria — when a learner has completed a co… | GET /api/academy/class-groups/[classGroup… | student|teacher|adm… | UNSET. Schema: {requireAllLessonsCompleted: boolean, minimumAttendedRatio: 0..1 or null, requiredAssessmentModes: list … | The academy states its completion standard once; programs and courses override it. | No default and no seed. | src/lib/academy/policies/registry.ts:146-151,335-353; src/lib/academy/services/progr… | P1 | No code change. PROPOSED STARTING SHAPE (not a recommendation of the numbers): {"req… | yes, emphatically — the pass mark, … | BLOCKED BY REQUIRED DATA | single-pass |
| G044 | certificate.eligibility — who may receive a certificate | GET + POST /api/admin/academy/enrollments… | admin | UNSET, so both checking eligibility and issuing return 503 (certificate-service.ts:50, reached from both eligibility() … | The academy states whether a certificate exists for a course and what earns it. | No default and no seed. | src/lib/academy/policies/registry.ts:153-157,354-366; src/lib/academy/services/certi… | P1 | No code change. PROPOSED INITIAL VALUE at academy scope: {"certificateOffered":false… | yes — whether a certificate is offe… | BLOCKED BY REQUIRED DATA | single-pass |
| G045 | recordings.access — learner access to session recordings | GET /api/academy/class-groups/[classGroup… | student | UNSET, so a LEARNER listing or opening recordings gets 503 (recording-service.ts:53, called only on the learner branch)… | The academy states whether enrolled learners may watch recordings, for how long, and… | No default and no seed. | src/lib/academy/policies/registry.ts:159-166,367-381; src/lib/academy/services/recor… | P1 | No code change. PROPOSED INITIAL VALUE: {"learnerAccess":"enrolled_learners","availa… | yes — redistribution of recorded re… | BLOCKED BY REQUIRED DATA | single-pass |
| G046 | communications.messaging — learner/teacher direct messa… | POST /api/academy/messages/threads (openi… | student|teacher | UNSET, so opening a student<->teacher conversation returns 503 (communication-service.ts:113). Admin conversations are … | The academy states the permitted direction(s) of learner/teacher messaging within an… | No default and no seed. | src/lib/academy/policies/registry.ts:168-176,382-393; src/lib/academy/services/commu… | P1 | No code change. PROPOSED INITIAL VALUE: {"learnerMayMessageTeacher":true,"teacherMay… | yes — direct adult/child or teacher… | BLOCKED BY REQUIRED DATA | single-pass |
| G047 | Approval gate definitions (a second, non-registry sourc… | GET + PUT /api/admin/academy/approval-gat… | admin | Approval gates are NOT registry policies: they live in academy_approval_gate_definitions and are configured through the… | The administrator's setup checklist covers everything that has no default, including… | Two independent no-default mechanisms share one error code, and only one of them is … | src/lib/academy/governance/approval-gates.ts:161-163; src/lib/academy/repo/gate-repo… | P1 | No code change needed to unblock: an administrator PUTs the publication gate definit… | yes — how many approvals a publicat… | BLOCKED BY REQUIRED DATA | single-pass |
| G048 | Teacher session-preparation privacy | /api/academy/sessions/[sessionId]/prepara… | teacher | CONFIRMED. `session.prepare` returns `deny('role')` for anyone who is not a teacher, administrators included, then requ… | A teacher's preparation notes are their own work; administrators see status through … | n/a. | src/lib/academy/permissions/permissions.ts:198-203; src/lib/academy/services/lesson-… | P1 | None. | no | WORKING | confirmed |
| G049 | Cloudinary signed upload authorisation | POST /api/cloudinary/sign-upload; POST/PA… | applicant | CONFIRMED; only the route's line range was wrong (the handler is sign-upload/route.ts:55-106, not :43-105). The body ma… | Uploads are type-, size-, ownership- and delivery-constrained, and the signing route… | n/a. | src/app/api/cloudinary/sign-upload/route.ts:55-106 (503 gates at :75-83); src/lib/se… | P1 | None in code. Two residuals the code itself documents and cannot close: (1) `resourc… | yes - confirm the two signed preset… | WORKING | confirmed |
| G050 | Whop environment defaults to the sandbox API base | src/lib/payments/whop.ts; POST /api/acade… | system | CONFIRMED. `resolveWhopConfig` rejects any base that is not exactly the sandbox or production constant (whop.ts:46-48),… | Real money flows only through the production base; a mismatched environment fails au… | Deliberate fail-safe default. It becomes a hazard only if a production API key is su… | src/lib/payments/whop.ts:6-10, :24-25, :45-64 (the default at :49, the derivation at… | P1 | Configuration, not code: set WHOP_API_BASE_URL to the production base together with … | yes - confirm which Whop environmen… | BLOCKED BY CONFIGURATION | confirmed |
| G051 | API error disclosure | All routes via src/lib/api/handler.ts | public | CONFIRMED. `withApi` maps everything through `toErrorResponse` (handler.ts:37), whose default branch discards the error… | Clients see a status and a safe message; provider text, stack traces and internal id… | n/a. | src/lib/api/handler.ts:30-47; src/lib/auth/core.ts:113-121; src/lib/academy/permissi… | P1 | None. | no | WORKING | confirmed |
| G052 | Academy library reads three retired legacy tables | GET /api/academy/library/books/[bookId] a… | student | CONFIRMED. The repository layer's own header records the design - 'Books, pages and library entitlements are READ from … | Either the library reads academy_* tables, or the feature is explicitly unavailable … | The academy library layer was built to read the pre-existing library tables, and tho… | src/lib/academy/repo/library-repo.ts:1-7, :18-20, :31-38, :55-57 (retired tables) ve… | P1 | Not local. Either migrate book, page and entitlement storage into academy_* tables, … | yes - migrate the library onto acad… | BLOCKED BY SCHEMA | confirmed |
| G053 | Legacy messaging and chat authorisation | POST /api/messages, POST /api/chat/send, … | student | CONFIRMED, with two numeric corrections. `mayDirectMessage` applies the academy rule before any write and returns false… | No signed-in account can message or broadcast to someone it has no academic relation… | n/a. | src/lib/security/messaging-guard.ts:27-45; src/app/api/messages/route.ts:30-31, :58-… | P1 | None. The honest limitation recorded at chat/send/route.ts:28-36 stands: rate limiti… | no | WORKING | confirmed |
| G054 | Assessment answer-key protection | GET /api/exam/[courseId]/questions, /api/… | student | CONFIRMED on every traced path. The legacy exam route requires an enrolled student and wraps the result in `stripAnswer… | A learner never receives the correct answers with the questions. | n/a. | src/app/api/exam/[courseId]/questions/route.ts:11-22; src/app/api/quizzes/[lessonId]… | P1 | None for disclosure. The attempt-integrity gap at start-exam/route.ts:13-14 needs sc… | yes - attempt caps and question-set… | WORKING | confirmed |
| G055 | PAY-04 overall acceptance — Whop commerce slice | POST /api/academy/checkouts, GET /api/aca… | public|student|admi… | The full buying path exists in code (offer -> server-opened checkout -> Whop redirect -> signed webhook -> entitlement … | PAY-04 is accepted only when a real (sandbox first) Whop tenant has been driven thro… | The slice was built and unit-tested against an injected gateway and a fake SQL execu… | src/lib/academy/services/commerce-service.ts:256,263,286,302,340,360 (assertAcademyC… | P1 | NOT LOCAL. Acceptance requires: (1) ACADEMY_CORE_SCHEMA_READY=true, (2) Whop sandbox… | yes — whether PAY-04 can be accepte… | BLOCKED BY CONFIGURATION | single-pass |
| G056 | Environment values the commerce slice needs, and what e… | all commerce routes | system | Five values govern the slice. ACADEMY_CORE_SCHEMA_READY=true — without it every commerce call is 503 (flags.ts:19,23). … | Each value set deliberately, sandbox first, and a configured key understood as enabl… | n/a — this is the configuration contract. | src/lib/payments/whop.ts:39-65,142,153; src/lib/academy/server.ts:121-135; src/app/a… | P1 | NOT LOCAL. Set the four missing values against the Whop SANDBOX, leaving WHOP_API_BA… | yes — who holds the sandbox tenant … | BLOCKED BY CONFIGURATION | single-pass |
| G057 | Grant path depends on Whop propagating checkout metadat… | POST /api/academy/checkouts -> Whop -> PO… | system | createWhopCheckout POSTs to `${apiBase}/checkout_configurations` with plan_id, redirect_url, an Idempotency-Key equal t… | A sandbox purchase confirms the endpoint, the response shape, that metadata survives… | The Whop API contract is encoded from documentation, not from an observed exchange; … | src/lib/payments/whop.ts:137-172 (endpoint, idempotency key, metadata, response vali… | P1 | NOT LOCAL. This is the single make-or-break unverifiable assumption of PAY-04: drive… | yes — accept that PAY-04 cannot be … | INCOMPLETE IMPLEMENTATION | single-pass |
| G058 | Two open checkouts for one class group: double charge, … | POST /api/academy/checkouts -> POST /api/… | student | planOpenCheckout refuses a second purchase only when the learner already has an OPEN ENROLLMENT or an ACTIVE ENTITLEMEN… | Either a second concurrent checkout for the same learner and class group is refused … | Duplicate protection is keyed on the checkout and the payment, never on (learner, cl… | src/lib/academy/commerce/commerce.ts:211-213 (open-time check ignores existing check… | P1 | LOCAL (code only, no schema or production change needed for the second half). (a) In… | yes — whether a duplicate payment s… | INCOMPLETE IMPLEMENTATION | single-pass |
| G059 | Reconciliation and commerce administration — held, mism… | no route exists (academy_payment_events, … | admin | Every outcome that means 'money was taken but nothing was granted' — outcome 'held' with failure 'mismatch' or 'not_enr… | An administrator can see, per class group and per learner, the entitlements with the… | The slice was built for the buying path only; the administrative read side was never… | src/lib/academy/services/commerce-service.ts:181-204 (held outcomes recorded), :253-… | P1 | LOCAL. Add read-only admin endpoints and a screen: entitlements by class group (lear… | yes — the scope of the admin commer… | INCOMPLETE IMPLEMENTATION | single-pass |
| G060 | Homepage early-bird "50% OFF" capture form | / (src/app/page.tsx hero); no API endpoin… | public | the defect was real and exactly as described at e257693 — I read `git show e257693:src/app/page.tsx` and confirmed :140… | The site makes no discount or price promise that an approved commercial decision doe… | The submit handler was never wired to a backend, and no discount mechanism exists an… | git show e257693:src/app/page.tsx:140, :142-149, :145-146, :156-161 (verified); src/… | P2 | Already applied locally at 8908c57 and guarded by tests/security/truthful-public-cla… | yes — whether a launch discount exi… | FIXED LOCALLY | corrected |
| G061 | Homepage hardcoded statistics "30+ Years Academic Exper… | / (hero stat band) | public | CORRECTED (detail only): the substance holds and the claim is still live at HEAD, but the quoted code is wrong. The lin… | Any published figure is either verified by the owner or not shown. | Placeholder metrics left in place; no statistics service was ever built and /api/sta… | src/app/page.tsx:49-51, :54-56, :131-134, :137-140 (HEAD 8908c57; e257693 equivalent… | P2 | LOCAL. Replace with verified approved information (the owner states the real number … | yes — the real years of experience,… | OWNER DECISION REQUIRED | corrected |
| G062 | Homepage pre-launch promises attached to a list that do… | / ("Exclusive Sneak Peek" section) | public | CONFIRMED, with line numbers moved by 8908c57. At HEAD the two guarantees are src/app/page.tsx:247 `<T>No credit card r… | The site does not promise a notification or an access tier it cannot deliver. | Marketing copy written for a launch flow that was never implemented; the form was re… | src/app/page.tsx:244-253 (HEAD 8908c57; e257693 :282-291); src/app/api/waitlist/rout… | P2 | LOCAL. Temporary removal pending owner approval: delete src/app/page.tsx:244-253. Do… | yes — whether an early-access tier … | INCOMPLETE IMPLEMENTATION | confirmed |
| G063 | Homepage fake preview-video player | / ("Behind the scenes of Ruh-Ul-Qudus") | public | CONFIRMED, line numbers moved by 8908c57. At HEAD src/app/page.tsx:256-268 renders the `aspect-video` card with `cursor… | A play control is shown only when a video can be played. | Placeholder media block never replaced with a real asset or player. | src/app/page.tsx:256-268 (HEAD 8908c57; e257693 :294-306), body copy :239-243; `ls p… | P2 | LOCAL. Temporary removal pending owner approval: delete the section at src/app/page.… | yes — does a preview recording exis… | INCOMPLETE IMPLEMENTATION | confirmed |
| G064 | Homepage hero feature badges: certified teacher program… | / (hero card) | public | CONFIRMED as a copy/reality mismatch, line numbers moved by 8908c57. At HEAD src/app/page.tsx:167-169 carries the three… | Product capabilities are described in a tense that matches what a visitor can actual… | Copy describes the intended product while the catalogue answers "not available yet" … | src/app/page.tsx:166-181, :201-203 (HEAD 8908c57; e257693 :204-220, :239-241); src/a… | P2 | LOCAL wording change only. Phrase the badges as what the academy is building until t… | yes — whether pre-launch future-ten… | BLOCKED BY CONFIGURATION | confirmed |
| G065 | /assessments — a placement test advertised behind a dea… | /assessments | public | half of this is already fixed and committed, and the severity drops with it. Commit 8908c57 (now HEAD) replaced the fou… | A placement test is offered only where one can be taken, and sign-up is not sold on … | No placement service was ever built; the component set is itself a mock with a hardc… | src/app/assessments/page.tsx:33-37 (HEAD 8908c57, fixed), :47 (unfixed CTA); src/app… | P2 | LOCAL. Finish what 8908c57 started: correct the CTA at src/app/assessments/page.tsx:… | yes — whether a placement test is p… | INCOMPLETE IMPLEMENTATION | corrected |
| G066 | /contact renders raw keys, plus an unverifiable phone n… | /contact | public | CORRECTED on four line references, otherwise confirmed. The `<T>` key names render as claimed at src/app/contact/Contac… | Contact details, response-time commitments and social profiles are ones the owner ha… | Same `<T>` key-fallback defect; the `contactPage` namespace (src/messages/en.json:48… | src/app/contact/ContactContent.tsx:81, :85, :89, :108, :111, :122, :125-128, :139, :… | P2 | NOT LOCAL for the facts, LOCAL for the wiring. The owner must confirm the phone numb… | yes — phone, address, hours, respon… | OWNER DECISION REQUIRED | corrected |
| G067 | Navbar advertises a Library that tells signed-out visit… | /library (navbar "More" menu, src/compone… | public | CONFIRMED, with line corrections and one material addition to the fix. The signed-out early return is src/app/library/p… | A signed-out visitor is told sign-in is required; an unavailable library is never re… | The signed-out early return predates the `failed` flag added for the signed-in case … | src/app/library/page.tsx:54-58, :71, :121-127, :136, :169-174 (:172 the two-way bran… | P2 | LOCAL. Render a sign-in prompt for `!user` before the empty-state branch, adding a F… | yes — whether the public /library s… | INCOMPLETE IMPLEMENTATION | confirmed |
| G068 | Message dictionaries retain unsupported commercial and … | src/messages/{en,ar,tr}.json — loaded int… | public | CONFIRMED; I printed each cited line of src/messages/en.json and every one matches: :58 "+5% every 50 lessons at 4.5+ r… | Retracted or unapproved claims are removed from the dictionary, not parked in it. | The affiliate programme was converted to reward-free invitations and the bundle/comm… | src/messages/en.json:58, :118, :124, :130, :395, :463, :509, :536, :554, :562, :583 … | P2 | LOCAL. Delete the retracted claims from all three dictionaries — at minimum the 20% … | yes — which of the remaining claims… | OWNER DECISION REQUIRED | confirmed |
| G069 | Site-wide AI assistant invites visitors to ask about pr… | / and every page (src/components/shared/A… | public | CONFIRMED, plus one thing the finding missed. src/components/shared/AIChatBubble.tsx:9-14 is the SUGGESTIONS array with… | The assistant does not advertise an answer it has no data for. | The suggestion list and the per-course display instruction were written for the lega… | src/components/shared/AIChatBubble.tsx:9-14, :19-24 (:12, :22); src/app/api/ai/chat/… | P2 | LOCAL. Remove the price suggestion at src/components/shared/AIChatBubble.tsx:12 and … | no. | INCOMPLETE IMPLEMENTATION | confirmed |
| G070 | Colour contrast — the `--border` and `--input` tokens f… | global — every bordered input, card and b… | public | `--border` and `--input` are both oklch(0.88 0.02 240) ≈ #ccdae4. Against `--background` (#fbfcfd) that is 1.39:1 and a… | Input and control boundaries reach 3:1 against their adjacent background. | The border token was chosen for a soft visual rather than against the 1.4.11 thresho… | src/app/globals.css:82-83 `--border: oklch(0.88 0.02 240); --input: oklch(0.88 0.02 … | P2 | LOCAL. Darken `--border`/`--input` in light mode to about oklch(0.72 0.02 240) (≈3.1… |  | SECURITY DEFECT | single-pass |
| G071 | Responsive — a global `overflow-x: hidden` masks horizo… | global | public | globals.css sets `html, body { max-width: 100vw; overflow-x: hidden; }`. The browser suite's guard is `expectNoHorizont… | Layouts fit their viewport, and the test that checks this can actually fail. | A blanket suppression was added to hide overflow symptoms rather than fix their caus… | src/app/globals.css:140-146 `html, body { background-color: var(--background); color… | P2 | LOCAL. Remove `max-width: 100vw` and `overflow-x: hidden` from src/app/globals.css:1… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G072 | Keyboard — legacy profile form controls are unlabelled … | /profile/student, /profile/teacher, /prof… | student, teacher, a… | The shared `Field` renders `<label>` and the control as siblings: the label has no `htmlFor` and does not wrap `childre… | Each control has a programmatic label, a focus indicator that reaches 3:1, and an er… | Two components share the name `Field`. The academy one (src/components/academy/works… | src/components/profile/Field.tsx:7-14 the `Common` type — no `htmlFor`; :20-40 `<lab… | P2 | LOCAL. Add `htmlFor` to src/components/profile/Field.tsx's props and an `id` to each… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G073 | Localisation — assistive-technology labels across the p… | global header, dictionary, library, cours… | public | 29 `aria-label` attributes outside the academy directories are hardcoded string literals: "Change language", "Open menu… | Accessible names follow the reader's locale, like everything else on the page. | `<T>` only works for element children; props need the `useT()` helper, which was add… | src/components/LanguageSwitcher.tsx:71 `aria-label="Change language"`; src/component… | P2 | LOCAL. Replace the literals with `useT()` lookups and add the keys to the three mess… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G074 | Privacy and cookies — the site sets only necessary and … | /privacy | public | Three cookies are set in the running application: `__session` (httpOnly Firebase session, strictly necessary), `preferr… | The privacy notice names the cookies actually set and their purpose. No consent mech… | The cookie disclosure is a casualty of the /privacy placeholder-key defect, not a mi… | src/app/api/auth/session/route.ts:38 `response.cookies.set("__session", sessionCooki… | P2 | Not purely local. Fixing the key wiring is local; the cookie-disclosure wording itse… | yes — the cookie-disclosure wording… | INCOMPLETE IMPLEMENTATION | single-pass |
| G075 | SEO — the sitemap and llms.txt advertise routes that ar… | /library, /quran, /blog, /forgot-password | public | src/app/sitemap.ts advertises /quran (priority 0.9) and /library (priority 0.8). /library is a client-only component th… | A sitemap lists addresses that return indexable content. /library, whose tables are … | The sitemap and llms.txt were written against the pre-retirement route list and were… | src/app/sitemap.ts:13 `{ url: \`${baseUrl}/quran\`, ..., priority: 0.9 }`, :14 `{ ur… | P2 | LOCAL. Remove /library and /quran from src/app/sitemap.ts:13-14 until they serve ind… | yes — whether /library and /quran a… | BROKEN LEGACY FEATURE | single-pass |
| G076 | Localisation and RTL — /quran forces RTL for every read… | /quran, /quran/[surah]/[ayah] | public | The page hardcodes `dir="rtl"` on its container regardless of the reader's locale, so English and Turkish readers get a… | Direction follows the reader's locale; Arabic runs inside a non-Arabic document are … | The page was written Arabic-first and never reconciled with the cookie-driven locale… | src/app/quran/page.tsx:25 `<div className="min-h-screen bg-background py-10" dir="rt… | P2 | LOCAL. Drop the hardcoded `dir="rtl"` (or make it `dir="auto"` on the Arabic runs on… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G077 | SEO — most academy workspace and legacy dashboard route… | 93 page.tsx files, including all 41 acade… | student, teacher, a… | 93 of 121 non-API page files define neither `metadata` nor `generateMetadata`, and have no ancestor layout supplying it… | Public routes declare their own title, description and canonical; private routes dec… | Metadata was added route by route on the marketing pages and via layouts on the priv… | Enumerated by walking src/app for page.tsx without `export const metadata`/`generate… | P2 | LOCAL. Add a `src/app/live/layout.tsx` with `robots: { index: false, follow: false }… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G078 | Answering an oral-response assessment item; media in au… | /academy/assignments/[assignmentId] (lear… | student | An `oral_response` item gives the learner a plain URL text box and expects them to paste a link to an audio file they h… | A learner asked for a spoken answer can record or upload it in the page; an author a… | The Cloudinary signing route was scoped narrowly to the teacher-signup flow (closure… | src/components/academy/workspace/assessment-items.tsx:107-110 `case "oral_response"`… | P2 | NOT FULLY LOCAL. Adding `learner_audio_response` and `lesson_media` purposes to `UPL… | yes — whether learner-submitted med… | INCOMPLETE IMPLEMENTATION | single-pass |
| G079 | Price on the public purchase step | /academy/courses/[slug] — GET /api/public… | public | The Enrol section lists each offer with a free-text `label`, the class group name, start date and open/full state, then… | A visitor can see what a place costs before being handed to a payment provider. | Whop is the single source of truth for pricing by design, and the offer record was k… | src/app/academy/courses/[slug]/page.tsx:137-152 renders `offer.label`, `classGroupNa… | P2 | Needs approval on the approach. Cheapest local option: keep the administrator's free… | yes — whether the price is mirrored… | OWNER DECISION REQUIRED | single-pass |
| G080 | Registration for a class group that is not sold through… | /academy/courses/[slug] (public) ; enroll… | public | There are exactly two ways a learner becomes enrolled: an administrator enrolls them by hand from /academy/manage/class… | Where a class group admits learners without payment, a permitted visitor can registe… | Enrollment was modelled around payment and administrative action only; no request-to… | src/lib/academy/structure/delivery.ts:590-591 `ENROLLMENT_SOURCES = ["admin", "entit… | P2 | NOT LOCAL. A self-registration path needs a third enrollment source and, if requests… | yes — whether unpaid registration e… | APPROVED FUTURE WORK | single-pass |
| G081 | Administrator-initiated conversations | /academy/messages — POST /api/academy/mes… | admin | `messagingRequirement` returns "none" whenever either party is an administrator, so the server will happily open a thre… | Either an administrator can start a conversation from the interface, or the Messages… | The recipient picker was written around the two class-group-scoped cases and the uns… | src/lib/academy/communication/messaging.ts:74 `if (sender.role === "admin" || recipi… | P2 | LOCAL. Add an admin branch to `NewConversation` that picks a recipient from the exis… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G082 | Academy-wide announcements | /academy/manage/announcements — POST /api… | admin | Publishing a COURSE-scope or CLASS-GROUP-scope announcement fans out an `announcement` notification to every active lea… | Either an academy-wide announcement reaches people the same way a course-wide one do… | The fan-out query enumerates recipients by walking class groups, which has no natura… | src/lib/academy/services/communication-service.ts:263-266 `if (plan.record.scope ===… | P2 | LOCAL either way. Either add an `academy` branch to the fan-out query (every active … | yes — whether an academy-wide annou… | OWNER DECISION REQUIRED | single-pass |
| G083 | Account pickers in academy administration | /academy/manage/class-groups/[id] (teache… | admin | `PersonSelect` for learners and `usePeopleNames` both fetch `/api/admin/users`, which runs `SELECT firebase_uid, email,… | An administrator searches for the learner they mean; the request returns a bounded p… | The people directory endpoint is a legacy listing that was secured (admin-only) but … | src/app/api/admin/users/route.ts:10-18 the unbounded query ; src/components/academy/… | P2 | LOCAL. Add `?role=`, `?q=` and `?limit=`/cursor parameters to the existing route (st… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G084 | Remediation rule authoring (linking an assessment item … | /academy/manage/courses/[courseId]?tab=re… | admin | The rule form gives a proper select for the assessment, but the content item to assign on failure is a bare text box in… | The administrator chooses the practice item from a list of the course's published pr… | The form was built around the ids the API takes rather than the lists the administra… | src/components/academy/workspace/admin/course.tsx:331-333 `<TextInput id="rule-item"… | P2 | LOCAL. Replace the text box with a select populated from `adminApi.items` (filtered … |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G085 | Top-level public navigation pointing at retired features | /dictionary (and /api/words) — linked fro… | public | The primary navigation lists Dictionary as one of four top-level links alongside Home, Academy and Certification. `/api… | The public navigation offers only features the deployment can serve; a retired featu… | The legacy dictionary predates the academy and its tables were not carried into the … | src/components/Navbar.tsx:31-36 `baseLinks` includes `{ to: "/dictionary", label: "D… | P2 | LOCAL. Remove the Dictionary entry from `baseLinks` (and Library from `moreLinks` if… | yes — whether the dictionary and li… | BROKEN LEGACY FEATURE | single-pass |
| G086 | Public course page — class group information | /academy/courses/[slug] — GET /api/public… | public | The class-group section of the public course page shows a name, a start and end date, and an open/full badge. It does n… | A visitor can tell what a class group actually commits them to — when it meets — bef… | The public projection was deliberately minimised (the module header states that publ… | src/lib/academy/public/catalog.ts:1-8 the module contract, and :24-29 `PublicClassGr… | P2 | Needs approval on what may be public. The data exists (academy_sessions carries ever… | yes — whether session times and a t… | OWNER DECISION REQUIRED | single-pass |
| G087 | Practice content delivery — story, adventure and media … | /academy/class-groups/[classGroupId]/cont… | student | Only `activity` and `game` items post a result. `story`, `adventure` and `media` items render a reader/player with no s… | Either every practice kind records some form of completion, or the screens and the r… | Result recording was built on the scored-item path; the three narrative and media ki… | src/app/academy/(workspace)/class-groups/[classGroupId]/content/[itemId]/page.tsx:57… | P2 | Needs a small decision then a LOCAL change. If a completion with no score is accepta… | yes — whether finishing a story or … | INCOMPLETE IMPLEMENTATION | single-pass |
| G088 | Policy resolution engine (registry + resolver + academy… | All 8 policy-reading services; GET /api/a… | admin|teacher|stude… | Values live in one table, academy_policy_values, one row per (policy_key, level), with a partial unique index per level… | Fail closed with a safe 503 and never guess a default; resolve academy -> program ->… | By design. registry.ts:8-10 states the registry "deliberately contains NO default va… | src/lib/academy/policies/registry.ts:1-33; src/lib/academy/policies/resolver.ts:86-1… | P2 | None. The mechanism is correct. Note for operators: POLICY_UNCONFIGURED is ALSO rais… | no | WORKING | single-pass |
| G089 | attendance.requirement — minimum attended ratio | None. No route or service reads this key. | admin | UNSET, and nothing consumes it. Schema: {minimumAttendedRatio: number 0..1, or null} (registry.ts:275-283). Scopes: aca… | Either a service enforces an attendance requirement, or the key is not offered to ad… | Declared ahead of a consumer that was never written (or superseded by completion.cri… | src/lib/academy/policies/registry.ts:116-118,275-283 (only occurrences in src); src/… | P2 | OWNER DECISION, then LOCAL. Either (a) remove the key from POLICY_DEFINITIONS so the… | yes — remove it or specify what it … | INCOMPLETE IMPLEMENTATION | single-pass |
| G090 | assessment.requirements — required assessment modes | None. No route or service reads this key. | admin | UNSET, and nothing consumes it. Schema: {requiredModes: list of assessment modes from practice, quiz, homework, writing… | Either a service enforces it, or it is not offered. | Declared ahead of a consumer that was never written, or superseded by completion.cri… | src/lib/academy/policies/registry.ts:142-144,326-334 (only occurrences in src); src/… | P2 | OWNER DECISION, then LOCAL: remove the key, or name what it gates (which assessments… | yes — remove or specify. | INCOMPLETE IMPLEMENTATION | single-pass |
| G091 | Policy editor: raw JSON textarea, no per-policy form | /academy/manage/policies (PolicyEditor); … | admin | Every value is typed as raw JSON into a <TextArea> and JSON.parse'd in the browser; a parse failure shows only the gene… | An administrator who has never read registry.ts can set all twelve settings correctl… | The screen was built as a generic editor over a typed registry; no per-key UI was wr… | src/app/academy/(workspace)/manage/policies/page.tsx:112-197 (JSON.parse at :135-141… | P2 | LOCAL: render a per-key template (the exact JSON skeleton with every mandatory field… | no — presentation only, provided no… | INCOMPLETE IMPLEMENTATION | single-pass |
| G092 | A policy value that fails to parse cannot be repaired f… | /academy/manage/policies; PATCH /api/admi… | admin | When a stored value no longer parses, policyMap catches POLICY_UNCONFIGURED/CONFLICT and returns resolution {status:"in… | An administrator can correct an invalid stored value from the screen that reports it… | The 'invalid' resolution drops the trail, which is the editor's only source of the r… | src/lib/academy/services/admin-service.ts:99-103; src/app/academy/(workspace)/manage… | P2 | LOCAL: include the trail (which resolvePolicy has already built for the defined leve… | no — this is a defect, not a choice. | INCOMPLETE IMPLEMENTATION | single-pass |
| G093 | Scheduling a session before institution.timezone is set | /academy/class-groups/[classGroupId] (Sch… | admin | With no academy time zone the workspace provider carries timeZone=null (server.ts:184-189), so localToIso/wallClockInpu… | Either the form is disabled with the ZoneHint as the reason, or the refusal says the… | The client degrades the value to undefined instead of blocking the submit, so a conf… | src/components/academy/workspace/admin/class-group.tsx:206,218,306-319,592-603; src/… | P2 | LOCAL: disable the schedule and assignment-window submits when timeZone is null and … | no | INCOMPLETE IMPLEMENTATION | single-pass |
| G094 | Session revocation does not reach the Bearer ID-token p… | Any route calling requireAuth/requireRole… | admin | CONFIRMED as stated, and the live-path claim is stronger than the finding said. src/lib/auth/index.ts:38 is literally `… | Revoking an account's tokens at sign-out should refuse both credential kinds. | Asymmetric dependency wiring: `verifySessionCookie` is called with checkRevoked=true… | src/lib/auth/index.ts:37-40 versus :41-45; src/lib/auth/core.ts:155-159; src/compone… | P2 | LOCAL. In src/lib/auth/index.ts:38 change `verifyIdToken(token)` to `verifyIdToken(t… | no | SECURITY DEFECT | confirmed |
| G095 | Rate-limit key is derived from an attacker-controlled h… | All per-IP limiters: /api/signup/student,… | public | CONFIRMED with corrected line numbers and one new fact that strengthens it. `clientKey` is at rate-limit.ts:127-136 (no… | The abuse-control key is derived from a value the client cannot choose - the proxy-a… | The helper reads the first hop unconditionally, with no trusted-proxy count and no f… | src/lib/security/rate-limit.ts:127-136 (clientKey; the first-hop read is :130), modu… | P2 | LOCAL once one deployment fact is known: how many proxies sit in front of the app on… | yes - confirm the trusted-proxy dep… | SECURITY DEFECT | confirmed |
| G096 | Zoom recording pipeline depends on a retired table | POST /api/webhooks/zoom; POST /api/lesson… | system | CONFIRMED, all four cited behaviours verified. After signature verification the handler runs `SELECT id FROM lessons WH… | Either the route resolves a live academy session/recording, or it acknowledges and i… | The route still keys on the legacy `lessons` table; the academy's recording model (a… | src/app/api/webhooks/zoom/route.ts:56-65, :84-86, :111-114; src/app/api/lessons/[id]… | P2 | Not local - needs an owner decision. Either (a) retire this webhook and the upload-y… | yes - retire the Zoom/YouTube pipel… | BROKEN LEGACY FEATURE | confirmed |
| G097 | Authenticated local-disk upload has no quota or rate li… | POST /api/upload | student | CONFIRMED, and one fact the investigation missed makes the fix easier rather than harder. The route is `requireAuth` on… | Storage consumption by an ordinary account is bounded, and a route nothing calls is … | MAX_UPLOAD_BYTES bounds a single request but nothing bounds the number of requests; … | src/app/api/upload/route.ts:31-63 (requireAuth only, no limiter import); src/lib/sec… | P2 | LOCAL, and the cheapest correct fix is retirement: since no caller exists, return 41… | yes - retire /api/upload, or decide… | INCOMPLETE IMPLEMENTATION | confirmed |
| G098 | Content Security Policy permits inline script | All pages (next.config.ts headers()) | public | CONFIRMED as a fact, with two corrections to the header inventory. `script-src 'self' 'unsafe-inline'` with 'unsafe-eva… | A CSP that actually constrains injected script - nonce- or hash-based script-src. | `'unsafe-inline'` was kept to allow the app's inline scripts (comment at next.config… | next.config.ts:15-17, full set at :5-67, applied at :107-114; COOP value at :56-57; … | P2 | LOCAL but non-trivial: move to a per-request nonce (`script-src 'nonce-...' 'strict-… | no | INCOMPLETE IMPLEMENTATION | confirmed |
| G099 | Account creation has no human-verification control | POST /api/signup/student, POST /api/signu… | public | CONFIRMED with an important reachability correction. Neither account-creation route imports or calls any captcha helper… | Creating an account and triggering an outbound email is gated by something a script … | The captcha helper was applied to the two email-sending marketing routes but not to … | src/app/api/signup/student/route.ts:17-31 (no captcha import; only the limiter and t… | P2 | LOCAL. Require and verify `captchaToken` in both signup routes using the existing `v… | no | INCOMPLETE IMPLEMENTATION | confirmed |
| G100 | Webhook signature verification | POST /api/webhooks/whop | system | The route fails closed without a secret (500), reads the RAW body before parsing, verifies Standard Webhooks HMAC-SHA25… | Exactly this. | n/a | src/lib/payments/whop.ts:73-107 (verification), :26 (300 s tolerance); src/app/api/w… | P2 | None. One residual: the header names (`webhook-id`, `webhook-timestamp`, `webhook-si… |  | WORKING | single-pass |
| G101 | Event idempotency and replay | POST /api/webhooks/whop | system | handleWhopEvent short-circuits on a prior academy_payment_events row for the delivery id, then writes the event row and… | Exactly this. | n/a | src/lib/academy/services/commerce-service.ts:130-142,362; src/lib/academy/repo/comme… | P2 | None. Note the deliberate consequence: because the event row is written inside the e… |  | WORKING | single-pass |
| G102 | No money is recorded anywhere: amount, currency and pai… | POST /api/webhooks/whop -> academy_entitl… | system|admin | academy_offers stores a plan id and a label but no price. academy_entitlements stores the Whop payment, membership and … | Either the amount and currency of each successful payment are recorded so academy re… | Migration 0011 deliberately keeps financial data in Whop ('nothing here stores card … | db/migrations/0011_academy_commerce.up.sql:21-22,42-59 (offers: no price), :83-104 (… | P2 | LOCAL if the owner wants it: read amount and currency in readPayment, persist them o… | yes — is Whop the sole financial bo… | OWNER DECISION REQUIRED | single-pass |
| G103 | A membership event arriving before its entitlement exis… | POST /api/webhooks/whop (membership.deact… | system | onMembership loads entitlements by provider_membership_id; when none exist it commits outcome 'recorded' with detail 'n… | A membership event that finds no entitlement is either retried, or parked so that a … | The handler treats 'no matching entitlement' as a terminal, successful outcome, and … | src/lib/academy/services/commerce-service.ts:237-241; :362 (any recorded delivery id… | P2 | LOCAL. Either answer 409/500 so Whop retries while the entitlement is missing (bound… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G104 | Entitlements without a Whop membership id can never be … | POST /api/webhooks/whop (membership.deact… | system | provider_membership_id on an entitlement is copied from the payment payload and is nullable; readPayment returns null w… | Either every entitlement carries a membership id, or the product states that one-off… | The membership id is taken opportunistically from the payment payload and never reco… | src/lib/academy/commerce/commerce.ts:314,453 (membershipId may be null, copied strai… | P2 | NOT LOCAL to decide, LOCAL to implement once decided. First a sandbox capture showin… | yes — are academy offers one-off pu… | INCOMPLETE IMPLEMENTATION | single-pass |
| G105 | Capacity race on grant: a paid learner can end in an un… | POST /api/webhooks/whop (payment.succeede… | system|student | onPaymentSucceeded reads the class group's open enrollments OUTSIDE the transaction, hands that list to planGrant (whic… | A payment that cannot be enrolled is always recorded as 'held' with a failed checkou… | The pure capacity check runs against a snapshot taken before the lock; the authorita… | src/lib/academy/services/commerce-service.ts:190-193 (read outside the transaction),… | P2 | LOCAL. Catch DomainError CONFLICT around the grant commit in onPaymentSucceeded and … |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G106 | Access control on the commerce routes | POST /api/academy/checkouts; GET /api/aca… | public|student|admi… | Checkout open: requireAuth, then a per-uid rate limit of 10 per 10 minutes, then the service refuses any role other tha… | Exactly this. | n/a | src/app/api/academy/checkouts/route.ts:15-21; src/lib/academy/services/commerce-serv… | P2 | None. One note: the per-uid rate limiter is in-memory and per-instance (src/lib/secu… |  | WORKING | single-pass |
| G107 | Offer definition and its link to a class group and curr… | POST /api/admin/academy/courses/[courseId… | admin | An offer is one Whop plan id (validated against ^plan_[A-Za-z0-9]{1,64}$) mapped to exactly one class group of one cour… | Exactly this — it matches the locked blueprint (Class Group is the enrollment unit, … | n/a | src/lib/academy/commerce/commerce.ts:129-171 (plan id, class group and course checks… | P2 | None. Unverifiable part: nothing checks that the plan id typed in actually exists in… |  | WORKING | single-pass |
| G108 | Payment state transitions and what a checkout writes | POST /api/academy/checkouts; POST /api/we… | student|system | startCheckout writes an academy_checkouts row in state 'created' (with its audit event) BEFORE calling Whop, calls the … | Exactly this. | n/a | src/lib/academy/services/commerce-service.ts:325-335; src/lib/payments/whop.ts:160 (… | P2 | None required. One narrow residual (INFERENCE): if the gateway call succeeds but the… |  | WORKING | single-pass |
| G109 | Refund handling | POST /api/webhooks/whop (refund.created, … | system | A refund is matched to an entitlement by Whop payment id. A refund with status other than 'succeeded' is recorded witho… | Exactly this, plus an operator-visible record of partial refunds. | n/a for the logic; the visibility gap is the reconciliation finding above. | src/lib/academy/services/commerce-service.ts:224-235; src/lib/academy/commerce/comme… | P2 | None to the logic. Two caveats to record against acceptance: (1) amounts are compare… |  | WORKING | single-pass |
| G110 | Failure, timeout and retry behaviour of the webhook rec… | POST /api/webhooks/whop | system | Status codes are chosen to drive Whop's retries correctly: 500 when the secret is unconfigured, 401 for missing/stale/i… | Bounded body, and configuration errors that fail one feature rather than the whole A… | The webhook route was written outside withApi and without the size guard readJsonObj… | src/app/api/webhooks/whop/route.ts:25-65; src/lib/academy/http.ts:13-19 (the size gu… | P2 | LOCAL. Cap the raw body (reject over ~64 KB before hashing) and add a coarse per-IP … |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G111 | Legacy pre-Whop payment surfaces | /payment/success, /payment/cancel, /payme… | public|student | POST /api/payment/capture is correctly retired: it returns 410 with an explanatory comment saying entitlements are gran… | No page asserts a successful payment on arrival; the only payment result screen is /… | The P0 legacy retirement pass covered /cart and /courses but not /payment/*. | src/app/api/payment/capture/route.ts:3-12 (correctly 410); src/app/payment/success/p… | P2 | LOCAL. Replace the three /payment/* pages with redirect('/academy'), the treatment /… |  | BROKEN LEGACY FEATURE | single-pass |
| G112 | Homepage fabricated testimonials with five-star ratings | / ("What our students say" section) | public | the finding was accurate to the line at e257693 — :33 `// Static Testimonials`, :34-53 the three-person array ("Ahmed A… | Only testimonials the owner can evidence are published; ratings come from real submi… | Design-phase placeholder content; no reviews table exists in the academy schema and … | git show e257693:src/app/page.tsx:33-53, :530-575, :553-555 (verified); src/app/page… | P3 | Already applied locally at 8908c57 and guarded by a test. If the owner supplies real… | yes, but only prospectively — wheth… | FIXED LOCALLY | corrected |
| G113 | public/llms.txt makes an accreditation claim and links … | /llms.txt (static, served to crawlers and… | public | CONFIRMED. public/llms.txt:15 is `- [Blog](https://ruhulqudus.com/blog)` and :28 is "The platform offers interactive le… | The machine-readable site description lists real pages and makes no unbacked claim. | Static file not updated when the blog was dropped and the legacy feature set retired. | public/llms.txt:9, :10, :15, :21, :28; `ls src/app/blog` → no such directory; src/ap… | P3 | LOCAL. Remove the /blog line and the word "accredited" from public/llms.txt, reconsi… | no for the dead link; the accredita… | BROKEN LEGACY FEATURE | confirmed |
| G114 | Dead marketing components still carrying prices, discou… | No route — unreferenced modules under src… | public | CONFIRMED with three corrections, one of which widens the finding. Verified dead: BundleOffers, LibraryView, Certificat… | Retired commercial copy does not survive in the tree where a future import would rep… | The legacy course/bundle/library storefront was retired at the route level (pages re… | src/components/student/BundleOffers.tsx:6-34; src/components/library/LibraryView.tsx… | P3 | LOCAL. Delete the unreferenced modules along with the retired routes they served, in… | no — these belong to already-retire… | BROKEN LEGACY FEATURE | confirmed |
| G115 | Legacy public certificate-verification route still quer… | /verify/[id] | public | CONFIRMED with a line correction. The query is src/app/verify/[id]/page.tsx:22-34 (the finding said :21-33): `FROM cert… | One public verification route, backed by the academy certificate service. | Legacy route left behind when certificate verification moved to the academy service … | src/app/verify/[id]/page.tsx:13-19, :22-34, :45-51; src/app/api/certification/route.… | P3 | LOCAL. Retire /verify/[id] with a redirect to /academy/certificates/verify?code=…, a… | no. | BROKEN LEGACY FEATURE | confirmed |
| G116 | /affiliate — invitation page with rewards claim already… | /affiliate | public | CONFIRMED exactly, every line checked. src/app/affiliate/AffiliateContent.tsx:73 "Share your personal link. When a frie… | As observed. | Not a defect. Recorded so the audit's coverage of the 20%-commission claim is comple… | src/app/affiliate/AffiliateContent.tsx:18-20, :73, :76, :124, :151; src/app/affiliat… | P3 | No change to the page. The matching dictionary entries should be deleted (covered by… | no. | WORKING | confirmed |
| G117 | Sitemaps and hreflang advertise routes and locales that… | /sitemap.xml, root metadata | public | CONFIRMED with one line correction and one caveat. public/sitemap.xml:3-13 lists 11 URLs including /assessments (:12) a… | Indexed URLs resolve to working pages and hreflang points at real localized URLs. | The static sitemap predates the generated one, and the hreflang block was written fo… | public/sitemap.xml:3-13; src/app/sitemap.ts:8-21; src/app/layout.tsx:34-41, :88; `ls… | P3 | LOCAL. Delete public/sitemap.xml so the generated route is unambiguously authoritati… | no. | INCOMPLETE IMPLEMENTATION | confirmed |
| G118 | Colour contrast — three secondary token pairs land betw… | global | public | Computed from the tokens: dark-mode `--primary-foreground` on `--primary` is 4.19:1 (every filled primary button label … | Token pairs used for control labels and body text reach 4.5:1 in both themes. | The dark-mode primary was lightened to oklch(0.55) to read as text on a dark backgro… | src/app/globals.css:104 `--primary: oklch(0.55 0.14 250);`, :105 `--primary-foregrou… | P3 | LOCAL. Darken dark-mode `--primary` to about oklch(0.50 0.14 250) for a 4.9:1 button… |  | SECURITY DEFECT | single-pass |
| G119 | Dead localisation infrastructure — four translation mec… | n/a (build and maintenance) | system | next-intl ^4.13.0 is a dependency and src/i18n/ contains config.ts, routing.ts, navigation.ts, request.ts and translati… | One localisation mechanism, one key shape, and locale files that are provably in par… | Successive localisation attempts were layered without removing the previous one. | package.json:81 `"next-intl": "^4.13.0"`; src/i18n/routing.ts:3-7 `defineRouting({ l… | P3 | LOCAL. Pick one mechanism, delete src/components/LanguageProvider.tsx and the unwire… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G120 | Exercise blocks inside a Lesson Sheet | /academy/class-groups/[classGroupId]/less… | student | An `exercise` block renders its questions with a decorative `○` glyph beside each option; the options are plain text sp… | Either the exercise block is answerable and self-marking within the sheet, or it is … | Lesson Sheet exercises were modelled as canonical authored content (answers stripped… | src/app/academy/(workspace)/class-groups/[classGroupId]/lessons/[lessonId]/page.tsx:… | P3 | LOCAL either way. Making it answerable in-sheet is a larger change (it would need a … | yes — whether Lesson Sheet exercise… | OWNER DECISION REQUIRED | single-pass |
| G121 | Policy count in the review brief and the audit | src/lib/academy/policies/registry.ts | system | POLICY_DEFINITIONS contains exactly TWELVE entries, not thirteen: institution.timezone, attendance.vocabulary, attendan… | One accurate count everywhere, so the administrator's checklist is complete and fini… | Transcription error in the audit summary, carried into the task brief. | src/lib/academy/policies/registry.ts:264-394 (12 `define<...>({` entries); docs/audi… | P3 | LOCAL (documentation only): correct docs/audits/2026-09-21-unavailable-features.md:1… | no | WORKING | single-pass |
| G122 | Lesson Sheet availability list is 503 for administrator… | GET /api/academy/class-groups/[classGroup… | admin | getSheet exempts administrators from the missing-time-zone failure (releaseFor catches POLICY_UNCONFIGURED and returns … | Consistent administrative visibility: either both admin paths degrade to 'no release… | The catch was added on the single-sheet path only. | src/lib/academy/services/lesson-sheet-service.ts:110-123 vs :183-186; src/app/api/ac… | P3 | LOCAL and small: for user.role === "admin", catch POLICY_UNCONFIGURED in listAvailab… | no | INCOMPLETE IMPLEMENTATION | single-pass |
| G123 | CSRF / request-origin controls on state-changing routes | All POST/PUT/PATCH/DELETE routes | student | CORRECTED - the absence is real, the severity was too high, and one supporting argument is wrong. I reproduced both sca… | State-changing requests are rejected unless they originate from this site - ideally … | The design relies wholly on SameSite=Lax; no server-side origin assertion was ever a… | grep for origin/referer/sec-fetch-site/csrf across src: zero hits. Cookie attributes… | P3 | LOCAL. Add an origin assertion inside `withApi` (src/lib/api/handler.ts:30-47): for … | no | INCOMPLETE IMPLEMENTATION | corrected |
| G124 | Rate-limit store is per-process and in-memory | All 27 rate-limited routes | system | CONFIRMED with minor line corrections. The module note is at rate-limit.ts:4-8; `createRateLimiter` spans :56-103 (not … | Abuse limits hold across restarts and across instances if the deployment ever scales… | Deliberate, documented design decision for the current single-instance deployment. | src/lib/security/rate-limit.ts:4-8, :56-103, :106. Route inventory reproduced: 27 fi… | P3 | None now. If the Coolify deployment is ever given more than one replica, or restarts… | yes - confirm the deployment stays … | APPROVED FUTURE WORK | confirmed |
| G125 | Rate-limit coverage on authenticated academy write endp… | src/app/api/academy/** except /checkouts … | student | CONFIRMED. Only the cost-bearing academy routes carry a limiter: checkouts 10 / 10 min per uid (checkouts/route.ts:8, :… | An authenticated caller cannot generate unbounded database writes. | Rate limiting was applied to cost-bearing and abuse-prone routes (AI providers, emai… | src/app/api/academy/checkouts/route.ts:8, :16; src/app/api/academy/messages/threads/… | P3 | LOCAL. Add a shared per-uid write limiter in `withApi` or in the academy service sup… | yes - what per-caller write budget … | INCOMPLETE IMPLEMENTATION | confirmed |
| G126 | Zoom webhook has no delivery-id record; the downstream … | POST /api/webhooks/zoom -> POST /api/less… | system | CORRECTED - the defect is real but it is unreachable today and the severity was too high; one mechanism detail is stron… | Each Zoom delivery has exactly one effect, as the Whop receiver achieves with its tr… | No delivery-id/idempotency record on the Zoom path; the downstream guard is a TOCTOU… | src/app/api/webhooks/zoom/route.ts:33-115 (no dedupe record; unawaited internal call… | P3 | LOCAL, but only worth doing as part of whatever decision resolves the `lessons` depe… | no | INCOMPLETE IMPLEMENTATION | corrected |
| G127 | Unauthenticated file serving from the local upload root | GET /api/uploads/[...path] | public | CORRECTED - the mechanism is exactly as described, but the classification and severity were wrong once the consumers ar… | An explicit, written decision about whether files under uploads/ are public, with th… | The Phase 3 hardening pass addressed traversal, content type and sniffing on this ro… | src/app/api/uploads/[...path]/route.ts:17-48 (no auth import; `Cache-Control: privat… | P3 | Decide and then make the code say it. If these files are public course media (which … | yes - are files under uploads/ publ… | OWNER DECISION REQUIRED | corrected |
| G128 | Server-side logging of identity and provider data | src/lib/api/handler.ts; several route cat… | system | CONFIRMED with one line correction. `withApi` logs the whole caught error object on every 500: `console.error('[api] ' … | Logs carry conditions and correlation ids, not personal data or provider payloads; a… | Generic `console.error(..., error)` at the two catch-all boundaries. | src/lib/api/handler.ts:38-43; src/app/api/lessons/[id]/upload-youtube/route.ts:54-56… | P3 | LOCAL. In `withApi`, log `error.name` plus a correlation id rather than the whole ob… | yes - how long are application logs… | INCOMPLETE IMPLEMENTATION | confirmed |
| G129 | Academy workspace pages carry no server-side gate | /academy/** (src/app/academy/(workspace)/… | public | CONFIRMED. I read both layouts in full. The workspace layout (28 lines) imports no auth module and only resolves the lo… | An explicit decision. Data is protected either way; what is disclosed is the shape o… | Deliberate: authorisation is placed entirely at the API, with no middleware and no p… | src/app/academy/(workspace)/layout.tsx:10-28; src/app/academy/(workspace)/manage/lay… | P3 | LOCAL. Add a server-side `getSession` + `redirect(accountHome(...))` in the workspac… | yes - is exposing the administrativ… | INCOMPLETE IMPLEMENTATION | confirmed |
| G130 | Library page-range enforcement relies on unsigned stora… | GET /api/academy/library/books/[bookId] a… | student | CORRECTED - the code reading is right, the second bug is confirmed, and the severity should be P3 because the path is u… | A page a reader may not open must not be fetchable by URL either - signed, expiring … | The range check protects the API response but not the delivery of the bytes it point… | src/lib/academy/services/library-service.ts:146-167; src/lib/academy/library/access.… | P3 | Not local - needs a storage decision first. Either proxy page bytes through the gate… | yes - how library page images are s… | INCOMPLETE IMPLEMENTATION | corrected |
| G131 | Retired high-risk endpoints | /api/payment/capture, /api/student/enroll… | public | CORRECTED - the containment is real but the stated load-bearing property is inaccurate. I read all nine files. Four of … | A retired dangerous endpoint cannot run any of its former side effects. | n/a. | src/app/api/payment/capture/route.ts:1-12 (one import: NextResponse); src/app/api/st… | P3 | None. If the 'no imports' property is meant to be the invariant, state it precisely … | no | INTENTIONALLY RETIRED | corrected |
| G132 | Public certificate verification | GET /api/public/certificates/[code] | public | CONFIRMED with a small line correction. The route is unauthenticated by design and rate-limited at 30 / 10 min per clie… | Anyone holding a certificate code can verify it; nobody can enumerate certificates o… | n/a. | src/app/api/public/certificates/[code]/route.ts:7-19; src/lib/academy/certificates/c… | P3 | None. Record explicitly that this route discloses a learner's name and course to any… | yes - confirm that disclosing learn… | WORKING | confirmed |
| G133 | Firebase project consistency between client and Admin S… | src/lib/firebase/client.ts, src/lib/fireb… | system | CONFIRMED. The browser config is built from the six NEXT_PUBLIC_FIREBASE_* values (client.ts:11-18) and validated only … | A mismatch is caught with a clear message at startup rather than surfacing as univer… | No cross-check between the two configuration sources. | src/lib/firebase/client.ts:11-41; src/lib/firebase/admin.ts:9-35. | P3 | LOCAL. At server startup, compare `JSON.parse(FIREBASE_ADMIN_KEY).project_id` with `… | no | INCOMPLETE IMPLEMENTATION | confirmed |
| G134 | Checkout lifecycle: no cancel URL, no abandonment, no e… | POST /api/academy/checkouts, /academy/che… | student | createWhopCheckout sends only redirect_url; no cancel URL is passed, so Whop has nowhere to send an abandoning buyer an… | Either an explicit abandonment path (cancel URL plus a terminal state), or a documen… | Only the happy path and provider-signalled failure were modelled. | src/lib/payments/whop.ts:151-159 (redirect_url only, no cancel url); src/lib/academy… | P3 | LOCAL for the cosmetic half (stop polling after a bounded time and tell the learner … | yes — should abandoned checkouts ex… | INCOMPLETE IMPLEMENTATION | single-pass |
| G135 | Test harness reachability for the commerce suite | n/a (repository tooling) | system | package.json defines only dev, build, start and lint — there is no test script. The commerce tests run only through .gi… | A discoverable `npm test`, and eventually a blocking typecheck. | The test suite was added after package.json scripts were last touched; the typecheck… | package.json scripts block (dev/build/start/lint only); .github/workflows/ci.yml:26-… | P3 | LOCAL. Add "test": "node --test \"tests/**/*.test.ts\"" to package.json so the suite… |  | INCOMPLETE IMPLEMENTATION | single-pass |
| G136 | Buy button reports an unavailable academy as a generic … | /academy/courses/[slug] -> POST /api/acad… | student | startCheckout calls assertAcademyCoreAvailable BEFORE checking whether Whop is configured, so with the flag unset the r… | A 503 says plainly that online payment is not open yet and that no payment was taken… | The button's status mapping has no 503 branch and discards the server's message for … | src/components/academy/buy-button.tsx:41-47; src/lib/academy/services/commerce-servi… | P3 | LOCAL. Add a 503 branch that shows the server's message (it is already end-user safe… |  | INCOMPLETE IMPLEMENTATION | single-pass |

## Coverage notes from each investigation

### policies

COVERAGE. Read in full and traced: src/lib/academy/policies/registry.ts (all 12 definitions and every validator), policies/resolver.ts, services/policy-lookup.ts, services/policy-service.ts, repo/policy-repo.ts, db/migrations/0001_academy_governance_foundation.up.sql (table, constraints, indexes), lessons/release.ts, services/lesson-sheet-service.ts, services/admin-service.ts, repo/admin-repo.ts, infra/flags.ts, domain/errors.ts, and every consumer found by grepping effectivePolicy/academyTimeZone: assessment, attendance, certificate, communication, progress, recording services. Also read the admin policy screen, both policy API routes, workspace/format.ts, wall-clock-input.ts, server.ts academyDisplayTimeZone, and the three policy/release test files. No database, network, .env or Siyadah_* file was touched; nothing was edited.

CORRECTION TO THE BRIEF (hard evidence). The registry defines TWELVE policies, not thirteen — registry.ts:265,270,275,284,296,308,317,326,335,354,367,382. The audit is self-inconsistent on this (docs/audits/2026-09-21-unavailable-features.md:170 says 13; F024 at :39 says twelve). Everything else in the brief's ground truth held: no key carries a default (registry.ts:8-10), and there is no INSERT INTO academy_policy_values anywhere in db/ or scripts/, so a freshly migrated database has zero rows.

HOW academy_policy_values WORKS, in one paragraph. One row per (policy_key, level): partial unique indexes enforce one academy row per key, one row per (key,

### claims (adversarially verified)

VERDICT SUMMARY: 0 DROPPED, 18 CONFIRMED, 5 CORRECTED (findings 1, 2, 9 changed classification/severity; 13, 15 corrected for line accuracy). No claim was fabricated. Every cited file was opened; key-absence claims for /about, /certification and the legal pages were re-derived independently by loading src/messages/en.json in node rather than trusting the grep.

THE BIGGEST THING THE INVESTIGATION MISSED: HEAD MOVED DURING THE AUDIT. The repository is no longer at a0ed893. Commit 8908c57 "Say only what is true on the public pages" is now HEAD, and it committed the working-tree edits the investigation saw mid-flight. It removed the early-bird 50% form and the fabricated testimonials from src/app/page.tsx (100 lines deleted), replaced the `href="#"` placement-test links, added the three new strings to all three locales, and added tests/security/truthful-public-claims.test.ts (89 lines). Findings 1 and 2 are therefore FIXED LOCALLY, not open defects, and finding 9 is half-fixed. Any consumer of this audit that still lists "the homepage offers a 50% discount" as an open P1 will be wrong about the current tree. All my homepage line numbers are against HEAD 8908c57, with the e257693 equivalents given alongside.

THE NEW TEST ALREADY CLOSES TWO OF THE AUDIT'S TEST GAPS. tests/security/truthful-public-claims.test.ts:34-59 asserts the homepage contains no `50%|discount|early-bird`, no `alert(`, no `<form`, no `type="email"`, no `<Star` and none of the three testimonial names; :62-77 wa

### whop

COVERAGE. I read end to end: src/lib/payments/whop.ts (config, signature, checkout creation), src/lib/academy/commerce/commerce.ts (the whole pure domain, 535 lines), src/lib/academy/services/commerce-service.ts (388 lines), src/lib/academy/repo/commerce-repo.ts, the enrollment/capacity queries it depends on in src/lib/academy/repo/delivery-repo.ts, planEnroll/planEnrollmentStatus in src/lib/academy/structure/delivery.ts, db/migrations/0011_academy_commerce.up.sql and the class-group DDL in 0002, all five commerce HTTP routes plus the webhook route, the public course page, the return page, the BuyButton, the admin offers UI wiring, src/lib/api/handler.ts, src/lib/academy/domain/errors.ts, src/lib/academy/services/support.ts, src/lib/academy/repo/audit-repo.ts, src/lib/security/rate-limit.ts, and both test files. I ran `node --test tests/academy/commerce.test.ts tests/security/truthful-commerce.test.ts` (read-only, fake executor, no network, no database): 40 pass, 0 fail.

ACCEPTANCE STATUS IN ONE LINE. PAY-04 is code-complete and unit-tested, and it is NOT acceptable yet. It has never been executed against Whop or against a live database, the administrative and reconciliation half does not exist, and two correctness defects (double checkout / refund-suspends-paid-access, and the unrecorded capacity-race 500 loop) touch money.

IMPLEMENTED AND TESTED (unit level, fake executor, hand-built payloads): signature verification including replay and rotation; event idempotency by del

### security (adversarially verified)

VERDICT SUMMARY. 32 findings verified against the code: 24 CONFIRMED, 8 CORRECTED, 0 DROPPED. Nothing was fabricated; the investigation's core conclusion - that identity, object-level authorisation, SQL parameterisation, error non-disclosure and secret handling are genuinely sound - survives adversarial checking. The corrections are: (1) admin route count 106 not 107; (2) CSRF downgraded P2 to P3 with one supporting argument struck; (3) Zoom idempotency downgraded P2 to P3 as unreachable while `lessons` is absent; (4) /api/uploads reclassified SECURITY DEFECT/P2 to OWNER DECISION REQUIRED/P3 once its consumers are traced; (5) library page delivery downgraded P2 to P3 for the same reachability reason; (6) retired endpoints - the "no imports at all" property is false for four of the nine files; (7) signup-captcha exposure is not live today because both routes 503; (8) numerous file:line corrections, listed per finding.

FILE:LINE ACCURACY. Most references were accurate. Materially wrong: rate-limit.ts `clientKey` is :127-136 not :118-136 (the quoted first-hop read is :130); sign-upload handler is :55-106 not :43-105; createRateLimiter is :56-103 not :56-110; uploads/[...path] handler is :17-48 not :17-44; communication-service `loadOwnThread` is :128-134 not :127-133; certificates `verificationResult` is :185-190 not :186-191; upload-youtube token-refresh throw is :54-56 not :55-58; lesson-repo annotation section comment is :181-183; upload-youtube internal-secret check is :129

### journeys

COVERAGE

I traced all four approved journeys through the code, reading pages, their client data hooks, the API routes behind them, and the services and repositories underneath. Everything below is from reading the repository at the working-tree state described (e257693 on top of 1de8fa2); no database, no network, no .env, no Siyadah_* file was touched, and nothing was edited.

WHAT IS IN PLACE AND WIRED (no finding raised)

PUBLIC: homepage (src/app/page.tsx:59-64, 337-380) reads /api/public/academy/catalog and links to /academy and to course pages; /academy (catalog) links programs and courses; program and course pages link back and forward correctly; the course page carries outline, class groups, offers and BuyButton; BuyButton POSTs /api/academy/checkouts and follows the returned Whop URL; the Whop return page /academy/checkout/[checkoutId] polls the server's own record and never claims success from the browser's arrival (src/lib/payments/whop.ts:153 builds the redirect to exactly that path). FEATURE_UNAVAILABLE is caught and rendered as a notice rather than a crash on all three public academy pages, and metadata correctly sets noindex for missing/unavailable programs and courses.

STUDENT: sign-up → OTP → verification → login → workspace is complete apart from the landing defect reported; enrollment arrives either from an administrator or from a signed Whop payment event (which activates the enrollment, src/lib/academy/commerce/commerce.ts:431 `activate: true`); learner

### i18n-a11y

SCOPE AND METHOD. Read-only; I made no edits, ran no state-changing git commands, opened no .env, contacted no database or network, and opened no Siyadah_* file. Working files went to the session scratchpad. Evidence is from reading source at revision e257693, from the bundled Next.js 16 docs under node_modules/next/dist/docs/, and from scripts that parse the repository's own files (message-key flattening, `<T>` literal extraction, and an OKLCH→sRGB→relative-luminance contrast calculator). The three stated ground-truth items I relied on without re-deriving are: the absent legacy tables, the missing production env vars, and the in-flight POST /api/auth/session fix, which I left alone.

COVERAGE. Localisation: all three message files (en 928 / ar 930 / tr 953 flattened keys), all 644 `<T>` literals across every .tsx, the four translation mechanisms, and the academy's separate PUBLIC_MESSAGES and WORKSPACE_MESSAGES catalogues. RTL: counted physical vs logical utilities across all .tsx and traced the specific components where the difference is visible. Responsive: read the breakpoint switch in the header, the global overflow rule, the fixed-pixel widths, and the existing browser suite. Keyboard and screen reader: skip link, landmarks, focus styles, live regions, dialog semantics, div-with-onClick, label association, across both the legacy site and the academy workspace. Contrast: computed every text-on-background and boundary pair in the token palette in both themes. SEO: metadat

---

سري للغاية — ملكية فكرية خاصة — يُمنع التداول أو النسخ أو النشر أو إعادة الاستخدام دون إذن كتابي من صاحبة المشروع
